resources:
  - name: nginx-resource
    type: image
    integration: "docker-subint"
    pointer:
      sourceName: "library/nginx"
    seed:
      versionName: latest

  - name: ecs-resource
    type: cluster
    integration: "ecs-iam-subint"
    pointer:
      sourceName : "testCL"
      region: "us-west-2"
