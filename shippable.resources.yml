resources:
  - name: nginx-image
    type: image
    integration: "docker-subint"
    pointer:
      sourceName: "library/nginx"
    seed:
      versionName: latest

  - name: ecs-cluster
    type: cluster
    integration: "ecs-iam-subint"
    pointer:
      sourceName : "testCL"
      region: "us-west-2"
