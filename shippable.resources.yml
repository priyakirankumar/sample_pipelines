resources:
  - name: time-trigger
    type: time
    seed: 
      interval: "*/2 * * * *"

  - name: nginx-image
    type: image
    integration: "docker-subint"
    pointer:
      sourceName: "ankul/nginx"
    seed:
      versionName: prod.8
