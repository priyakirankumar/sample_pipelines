resources:
  - name: nginx-image
    type: image
    integration: "docker-subint"
    pointer:
      sourceName: "library/nginx"
    seed:
      versionName: latest

  - name: sample-pipeline
    type: gitRepo
    integration: gitlab-subint
    pointer:
      sourceName: ankul/sample_pipeline
      branch: "bran/ch"
