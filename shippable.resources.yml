resources:
  - name: nginx-image-4
    type: image
    integration: "docker-subint"
    pointer:
      sourceName: "library/nginx"
    seed:
      versionName: latest

  - name: timer-4
    type: time
    seed:
      interval: "*/2 * * * *"
