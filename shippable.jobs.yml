jobs:
  - name: nginx-manifest-3
    type: manifest
    steps:
      - IN: nginx-image

  - name: ecs-deploy-3
    type: deploy
    steps:
      - IN: nginx-manifest-3
      - IN: ecs-cluster
      - TASK: managed
        deployMethod: blueGreen
