jobs:
  - name: nginx-manifest-3
    type: manifest
    steps:
      - IN: nginx-image-3
      - TASK: managed

  - name: release-job
    type: release
    steps:
      - IN: nginx-manifest-3
      - IN: version-resource
      - TASK: managed
        bump: rc
