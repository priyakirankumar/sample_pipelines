jobs:
  - name: nginx-manifest
    type: manifest
    steps:
      - IN: nginx-image

  - name: ecs-deploy
    type: deploy
    steps:
      - IN: nginx-manifest
      - IN: ecs-cluster
      - TASK: managed
        deployMethod: blueGreen
