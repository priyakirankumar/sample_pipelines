# Language setting
language: node_js

services:
  - couchdb
  - elasticsearch
  - memcached
  - mongo
  - mysql
  - neo4j
  - postgres
  - rabbitmq
  - redis
  - rethinkdb
  - selenium
  - riak
  - sqlite

build:
  ci:
    - echo "here"
