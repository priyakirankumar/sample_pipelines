# Language setting
language: node_js

services:
  - couchdb
  - elasticsearch
  - memcached
  - mongo
  - mysql
  - neo4j
  - postgres
  - rabbitmq
  - redis
  - rethinkdb
  - selenium

build:
  ci:
    - echo "here"
