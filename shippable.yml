# Language setting
language: node_js

services:
  - cassandra
  - couchdb
  - elasticsearch
  - memcached
  - mongo
  - mysql
  - neo4j
  - postgres
  - rabbitmq
  - redis
  - rethinkdb
  - selenium
  - riak

build:
  pre_ci_boot:
    image_name: drydock/u14nodall
    image_tag: prod
    pull: true
  ci:
    - echo "here"
