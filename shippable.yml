# Language setting
language: node_js

services:
  - riak
  - mysql

build:
  pre_ci_boot:
    image_name: drydock/u14all
    image_tag: prod
    pull: true
    
  ci:
    - echo "here"
    - riak ping
