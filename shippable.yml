# Language setting
language: node_js

services:
  - cassandra
  - couchdb
  - elasticsearch
  - memcached
  - mongo
  - mysql
  - neo4j
  - postgres
  - rabbitmq
  - redis
  - rethinkdb
  - selenium
  - riak
  - sqlite

build:
  pre_ci_boot:
    image_name: drydock/u14all
    image_tag: prod
    pull: true
  ci:
    - echo "here"
