# Language setting
language: node_js

build:
  pre_ci_boot:
    image_name: drydock/u14nodall
    image_tag: prod
    pull: true
  ci:
    - echo "here_askask_1"
    
integrations:
  notifications:
    - integrationName: webhook-subint
      type: webhook
      payload:
        - versionName=$BUILD_NUMBER
      on_success: always
