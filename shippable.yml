# Language setting
language: python

# Version number
python:
  - 2.7

build:
  ci:
    - python --version
    - echo $COMMIT_MESSAGE
    - echo $COMMITTER
    - echo $LAST_AUTHOR
    - echo $BRANCH
    - echo $REPO_NAME
    - echo $REPO_FULL_NAME
    - echo $ORG_NAME
    - echo $RELEASE_NAME
    - echo $GIT_TAG_NAME
    - echo $BASE_BRANCH
    - echo $HEAD_BRANCH
    - echo $TRAVIS_BRANCH
    - echo $PULL_REQUEST_BASE_BRANCH
