# Language setting
language: python

# Version number
python:
  - 2.7

build:
  ci:
    - python --version
    

integrations:
  notifications:
  
    - integrationName: webhook-subint-2
      type: webhook
      payload:
        - versionName=$BUILD_NUMBER 
      on_start: always
